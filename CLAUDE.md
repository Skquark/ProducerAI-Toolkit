# Agent Notes

This repository uses `cli.js` as the canonical command interface.

Preferred validation commands:

```bash
node cli.js --help
npm test
```

Primary docs:

- `README.md`
- `QUICKSTART.md`

If command behavior changes, update those docs in the same change.
