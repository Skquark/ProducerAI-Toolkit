# Workflow Notes (Archived)

This document has been superseded.

Use:

- `README.md` for full instructions
- `QUICKSTART.md` for urgent backup flow

Canonical command reference:

```bash
node cli.js --help
```
