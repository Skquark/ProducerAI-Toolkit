# Environment Variables for Producer.ai Scraper

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# Browser profile path (optional - auto-detected if not set)
# BROWSER_PROFILE_PATH=/path/to/browser/profile

# Output directory (optional - defaults to ./output)
# OUTPUT_DIR=/path/to/output

# Checkpoint directory (optional - defaults to ./checkpoints)
# CHECKPOINT_DIR=/path/to/checkpoints

# Maximum concurrent downloads (1-5)
MAX_CONCURRENT_DOWNLOADS=2

# Retry attempts for failed operations
MAX_RETRIES=3

# Delay between songs in milliseconds
DELAY_BETWEEN_SONGS=2000

# Enable screenshot on error (true/false)
SCREENSHOT_ON_ERROR=true

# Session checkpoint interval (save progress every N songs)
CHECKPOINT_INTERVAL=10

# Rate limiting - requests per minute
RATE_LIMIT_RPM=30