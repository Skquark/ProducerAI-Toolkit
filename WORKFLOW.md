# Workflow Notes

This file is kept for compatibility with older links.

Current documentation lives in:

- `README.md` (full usage)
- `QUICKSTART.md` (fast path)

For current commands, always prefer:

```bash
node cli.js --help
```
